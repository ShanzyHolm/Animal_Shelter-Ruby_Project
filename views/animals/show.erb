<h1>All Animals</h1>

<div id="animals" class="grid">
<div class="grid-row">
  <div class="">

  </div>
<% @animals.each do |animal| %>
<figure style=" width: 300px;">
  <img src="/images/<%= animal.image %>" alt="Image of <%= animal.name %>" style="height: 200px;">
  <figcaption></figcaption>
  <ul>
    <li> <strong>Name:</strong> <%= animal.name %> </li>
    <li> <strong>Admission Date:</strong> <%= animal.admission %> </li>
    <li> <strong>Status:</strong> <%= animal.is_adoptable %></li>
    <li> <a href="/animals/<%= animal.id %> ">Show Animal Details</a></li>
  </ul>
</figure>
  <% end %>
</div>
</div>
<br>



<!-- <div class="grid">
  <div class="grid-row">
    <div class="cell">

      <% @animals.each do |animal| %>
      <p> Name: <%= animal.name %> </p>
      <p> Admission Date: <%= animal.admission %> </p>
      <p> <a href="/animal/<%= animal.id() %> ">Show Animal Details</a> </p>
    </div>

    <div class="cell">
      <img src="/images/<%= animal.image %>" alt="Image of <%= animal.name %>" height="45">
    </div>
  </div>
  <hr />
  <% end %> -->
